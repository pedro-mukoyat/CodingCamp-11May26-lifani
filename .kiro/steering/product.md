# Product

A mini project built as part of CodingCamp (11 May 2026) by lifani.

This is a lightweight, client-side web application with no backend. All data is persisted in the browser using the Local Storage API. The app runs entirely in the browser with no build step or server required.
